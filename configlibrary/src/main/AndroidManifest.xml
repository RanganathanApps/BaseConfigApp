<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="apps.ranganathan.configlibrary">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
            android:allowBackup="true"
            android:label="@string/app_name_base"
            android:supportsRtl="true"
            tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".activity.UtilActivity"/>

        <activity android:name=".base.BaseAppActivity"/>

        <activity android:name=".activity.AppImagePickerActivity"/>

        <activity android:name=".activity.PermissionsActivity"/>

    </application>

</manifest>